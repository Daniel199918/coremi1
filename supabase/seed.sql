-- ============================================================================
-- Données de développement local (exécuté par `supabase db reset`).
-- Ne JAMAIS y mettre de données de production ni de secrets.
-- ============================================================================

-- Exemple (décommenter au besoin) :
-- insert into public.profiles (id, email, display_name) values (...);
